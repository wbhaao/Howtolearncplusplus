#include <iostream>

int add(int a, int b) {
    return a+b;
}

// 함수
int main() {
    // 이건 변수
    int a = 1;

    // 모듈화!!
    // 기능 1 = 함수 1

    // 지역변수
    // 전역변수
    // 정적변수
    // 외부변수

    // 함수
    int data = add(10, 20);
    return 0;
}